Vagrant.configure("2") do |config|
   config.vm.box = "ubuntu_aws"
   config.vm.box_url = "https://github.com/mitchellh/vagrant-aws/raw/master/dummy.box"
   config.vm.synced_folder ".", "\vagrant",disable:true
   config.vm.provider :aws do |aws, override|	
    aws.access_key_id = "AKIAJ5XFGFGJUL2AMROA"
    aws.secret_access_key = "p3ZdlTO7AqEUg4AIQtat13+hHI6UhqlakhVX6GC3"	
    aws.keypair_name = "keyForAmazon"
    override.ssh.private_key_path = "./keyForAmazon.pem"
    override.ssh.username = "ubuntu"
    aws.instance_type = "t2.micro"
    aws.security_groups = "default"
    aws.region='ap-south-1'
    aws.ami = "ami-4a90fa25"
    aws.tags = {
      'Name' => 'Mysql-Second',
     }
  end

  config.vm.provision :puppet do |puppet|
    puppet.manifests_path = "C:\\Users\\singhv16\\Documents\\modules\\agility-mysql\\manifests"
    puppet.manifest_file  = "init.pp"
  end
end
